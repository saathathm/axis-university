# Axis University — Backend (Laravel API)

This repository contains the frontend (React/Vite) in `frontend/` and a Laravel API backend in `api/`.

This README describes the Laravel backend design required to support the frontend, including models, relationships, API endpoints, authentication (JWT), role-based access, database schema, folder layout, setup steps and recommended implementation notes.

(Full design previously generated; see `api/README.md` for quick setup and the project root for models/migrations.)
